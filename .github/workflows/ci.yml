name: CI

on:
  pull_request:
  push:
    branches: [main]

# CI only reads the checkout.
permissions:
  contents: read

# Supersede in-flight runs for the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: lint (ruff, black)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # No project dependencies needed, so skip the editable install entirely.
      # These versions are hand-copied from pyproject.toml's [project.optional-
      # dependencies].dev extra -- keep the two pins in step, or a pin bump in
      # one will not propagate to the other.
      - run: pip install ruff==0.16.2 black==26.5.1
      - run: ruff check src/ tests/
      - run: black --check src/ tests/

  typecheck:
    name: typecheck (ty)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml
      # ty resolves third-party imports through the environment, so the real
      # dependencies must be installed. The CPU index avoids a ~2.5 GB CUDA wheel.
      - run: pip install --extra-index-url https://download.pytorch.org/whl/cpu -e ".[dev]"
      - run: ty check src/ --output-format github

  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: pip install --extra-index-url https://download.pytorch.org/whl/cpu -e ".[dev]"
      - run: python -m pytest -m "not slow"

  smoke:
    name: smoke (bundled weights, non-blocking)
    runs-on: ubuntu-latest
    # IgLM and AntiBERTy are broken under transformers 5.x. This job reports
    # that breakage without blocking merges. With continue-on-error: true,
    # this job reports success to branch protection even when its tests
    # fail, so marking it "required" alone would not make it blocking.
    # Once the transformers-compatibility work lands, make it blocking by
    # removing continue-on-error below (only then does marking it required
    # in branch protection have any effect).
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: pip install --extra-index-url https://download.pytorch.org/whl/cpu -e ".[dev]"
      - run: python -m pytest -m smoke
